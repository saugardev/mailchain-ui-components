'use strict';

const reactUiComponents = require('..');
const assert = require('assert').strict;

assert.strictEqual(reactUiComponents(), 'Hello from reactUiComponents');
console.info('reactUiComponents tests passed');
